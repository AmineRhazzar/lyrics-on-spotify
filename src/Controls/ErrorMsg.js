import React from "react";

const ErrorMsg = (props) => {
  return (
    <div className="error">
      <p>Unfortunately, Spotify requires Premium for this action.</p>
    </div>
  );
};

export default ErrorMsg;
